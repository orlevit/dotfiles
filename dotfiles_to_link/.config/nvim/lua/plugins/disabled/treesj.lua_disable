return {
  'Wansmer/treesj',
  keys = { '<leader>gt', '<leader>gj', '<leader>gp' }, -- toggle, join, split
  dependencies = { 'nvim-treesitter/nvim-treesitter' }, -- if you install parsers with `nvim-treesitter`
  config = function()
    require('treesj').setup({--[[ your config ]]})
  end,
}
